# fullstackOS — Homebrew dependencies
# Usage: brew bundle --file=Brewfile

# Core tools
brew "git"
brew "python"
brew "jq"
brew "curl"
brew "gh"
brew "tmux"

# Runtime
brew "oven-sh/bun/bun"

# Optional: enhanced CLI experience
brew "fzf"
brew "ripgrep"

# CLIProxyAPI (Go proxy — installed separately via services/cliproxyapi/setup.sh)
# brew "your-org/tap/cliproxyapi"
